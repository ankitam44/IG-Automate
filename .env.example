# Copy to .env for local testing (never commit the real .env).
# In GitHub Actions these are set as repo secrets instead (Settings > Secrets > Actions).

GEMINI_API_KEY=
IG_BUSINESS_ACCOUNT_ID=
IG_ACCESS_TOKEN=
